// Drizzle schema barrel. Intentionally empty — real tables land in Sub-6
// (Supabase + Drizzle migration scaffold, ROA-93 series). drizzle-kit reads
// this file's exports to compute the schema diff, so keep the default export
// shape stable.

export const schema = {} as const;
export default schema;
